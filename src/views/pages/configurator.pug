extends ../layout/page-template

block head
  title Конфигуратор ПК

block head-scripts

block vars
  - var isInner = true; // раскрывать ли подменю в главном меню
  - var isTotalStyling = true; // Использовать бандл
  - var modHeader = '_breadcrumbs';

block caption
  h1.full-width Конфигуратор ПК

block inner-content
  section.c-configurator

    .c-configurator__header
      .c-configurator__main
        p Конфигуратор не дает 100% гарантии совместимости. Наши специалисты всегда рады помочь в подборе оптимальной конфигурации. <a href="#">Авторизуйтесь</a> в личном кабинете, чтобы сохранить ваши конфигурации.
      .c-configurator__aside
        a.button-gray.b-configurator__newconfig(href="#") Новая конфигурация

    .c-configurator__body
      .c-configurator__main
        .c-configurator__items
          +configurator-items()

        .c-configurator__additional-items
          +configurator-additional()
 
        .c-configurator__bottom-section
          .c-configurator__total
            +total()
      
      .c-configurator__aside
        .c-configurator__aside-plate
          
          .c-configurator__obligatory
            +configurator-obligatory()

          .c-configurator__consult
            +online-consult()

          .c-configurator__estimate
            +estimate()

          .c-configurator__aside-total
            +total_aside()

          .c-configurator__aside-guarantee Гарантия: 2 года при заказе сборки
        
        .c-configurator__aside-plate
          +configurator-save()

        .c-configurator__aside-plate
          +configurator-saved()

        //.c-configurator__link
        //  +configurator-link_aside()

        .c-configurator__socials
          +social-btns_aside()

    +expert-request()(id="expert-request-modal")